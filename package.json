{
    "scripts": {
        "prepare": "husky install",
        "frontend-lint": "cd frontend && npx next lint",
        "format": "prettier --ignore-path .gitignore \"**/*.+(ts|js|jsx|tsx)\" --write --tab-width 4"
    },
    "devDependencies": {
        "husky": "^7.0.1"
    }
}
