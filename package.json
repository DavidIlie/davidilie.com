{
    "scripts": {
        "prepare": "husky install",
        "lint": "cd frontend && npx next lint",
        "format": "prettier --ignore-path .gitignore \"**/*.+(js|tsx)\" --write --tab-width 4"
    },
    "devDependencies": {
        "husky": "^7.0.1"
    }
}
