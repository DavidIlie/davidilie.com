import { CustomPre, CustomCode, CustomDiv, CustomLink } from "./MDXComponents";

const MDXComponents = {
    pre: CustomPre,
    code: CustomCode,
    div: CustomDiv,
    a: CustomLink,
};

export default MDXComponents;
